; Platform admin backup upload-restore (large ZIP). Nginx/Caddy must also allow the body (e.g. client_max_body_size 512M).
upload_max_filesize = 512M
post_max_size = 512M
max_execution_time = 0
max_input_time = 600
