# Frontend production environment variables
# These are used during the build process in Docker

# API base URL (relative path for same-origin, or full URL for different domain)
VITE_API_BASE_URL=/api

# API URL (same as VITE_API_BASE_URL for same-origin deployments)
VITE_API_URL=/api
